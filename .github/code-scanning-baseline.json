{
  "$comment": [
    "The committed size of the code-scanning backlog, and the only thing that reads it is",
    "the `Code scanning backlog` job in security.yml.",
    "",
    "WHY A NUMBER AND NOT ZERO. `github/codeql-action/analyze` fails on nothing: it uploads",
    "every alert the security-and-quality suite raises and reports success either way. So the",
    "page has been accumulating findings that no gate has ever read, and demanding zero on the",
    "first run would red every pull request over a backlog none of them created — the failure",
    "mode the allocation gate exists to avoid, and the one that made the Semgrep job worthless",
    "for months.",
    "",
    "SO IT IS A RATCHET. The job fails when the open count exceeds `total`, which makes a new",
    "alert a failure today. Lowering the number is the work; raising it is a decision that has",
    "to be argued in the pull request that raises it, because nothing else here can move.",
    "",
    "HOW TO RE-RECORD. Read the count the job prints, fix alerts until it drops, and set",
    "`total` to what remains. Never set it to what the run reported without looking at the",
    "difference: a count that rose because a query pack was updated is a different fact from",
    "one that rose because somebody wrote a defect.",
    "",
    "`bySeverity` is not enforced. It is here so a re-recording says which way the backlog",
    "moved, rather than only that it did.",
    "",
    "WHAT `total` COUNTS. Alerts against files somebody wrote, on the ref the run analysed.",
    "Three quarters of this repository's alerts are raised against obj/generated — 748 of",
    "988 when it was last measured — and CodeQL cannot be told to skip them, because",
    "paths-ignore does not apply to a compiled language that is built. A ceiling over the",
    "whole page would therefore ratchet on generator output, where the fix is to change a",
    "generator that emits the same shape on purpose. The job prints both numbers and gates",
    "on this one.",
    "",
    "MEASURED, AND WHAT IT DOES NOT COVER. 235 of the 240 fall in six rules:",
    "cs/linq/missed-where 79, cs/path-combine 49, cs/catch-of-all-exceptions 35,",
    "cs/local-not-disposed 18, cs/linq/missed-select 18, cs/call-to-gc 9. The 35 are the",
    "justified CA1031 sites in FlowEngine, DurableLease and the hosted services and will",
    "not go to zero; the 9 are the allocation tests calling GC.Collect deliberately.",
    "Lowering this number means the LINQ and Path.Combine families, not those two.",
    "",
    "The ceiling binds on pushes to master and dev only, because a pull request analysis is",
    "annotated rather than persisted and has no alerts to count. See the backlog job."
  ],
  "total": 240,
  "bySeverity": {
    "error": 0,
    "warning": 46,
    "note": 194
  },
  "recordedAt": "2026-08-05",
  "recordedFrom": "refs/heads/dev, run 30982534985"
}
