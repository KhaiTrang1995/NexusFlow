# Architecture Decision Records

Every significant decision in FlowX has an ADR. One decision per record — never
bundled. Each record states the forces, the decision, the accepted trade-offs,
and the condition under which it must be revisited.

| ADR | Decision | Status | Revisit when |
|---|---|---|---|
| [0001](ADR-0001-flow-and-capability-as-primitives.md) | Flow + Capability are the only two user primitives | Accepted | a real use case cannot be expressed as either |
| [0002](ADR-0002-compile-time-orchestration.md) | Compile-time orchestration; no runtime reflection | Accepted | generator maintenance exceeds its benefit — measured as > 3 generator defects per phase (**counted by nothing**) or **build overhead > 8 % sustained, which has FIRED at +67.1 %** — or dynamic flows become a top-3 request |
| [0003](ADR-0003-execution-profiles.md) | Per-flow execution profiles instead of always-durable | Accepted | journal cost drops to ephemeral levels |
| [0004](ADR-0004-universal-trigger-model.md) | One trigger abstraction for all transports | Accepted | > 30 % of flows need transport-specific escape hatches |
| [0005](ADR-0005-manifest-as-build-artifact.md) | The manifest is a first-class build artifact | Accepted | never — this is foundational |
| [0006](ADR-0006-journal-and-leases.md) | Append-only journal + fenced leases | Accepted | a managed durable-execution primitive makes this commodity |
| [0007](ADR-0007-result-over-exceptions.md) | `Result<T>` for business outcomes | Accepted | the language gains first-class union types |
| [0008](ADR-0008-serialization-and-schema.md) | Source-generated STJ, versioned schemas | Accepted | journal serialisation becomes the measured bottleneck |
| [0009](ADR-0009-plugin-contracts.md) | Plugins depend only on `FlowX.Abstractions` | Accepted | never — extension integrity depends on it |
| [0010](ADR-0010-csharp-dsl-over-yaml.md) | C# DSL is the source of truth; YAML is export | Accepted | non-developer authoring becomes a primary requirement |
| [0011](ADR-0011-fixed-policy-stage-order.md) | Fixed policy stage order | Accepted | three documented legitimate counterexamples |
| [0012](ADR-0012-apache-2-license.md) | Apache-2.0 licence | Accepted | a governance model requires a different licence |
| [0013](ADR-0013-dsl-vocabulary-over-ca1716.md) | DSL vocabulary takes precedence over CA1716 | Accepted | a first-class VB.NET or F# consumer story is adopted |
| [0014](ADR-0014-derived-error-catalogue-vs-build-budget.md) | Keep the derived error catalogue; re-express the build-overhead budget | **Proposed** | four conditions, and **two are no longer open** ([§10](ADR-0014-derived-error-catalogue-vs-build-budget.md)#10-which-of-the-four-revisit-triggers-have-fired)): the inner loop pays full derivation cost per edit — **FIRED**; the withheld rate exceeds 20 % — **crossed at 42 %**, on evidence its own author calls inadmissible. Still open: a real project measures the derivation at > 2× its recorded cost, or P8 approaches the manifest v1.0 freeze |
| [0015](ADR-0015-journal-schema-and-durable-execution.md) | Journal the step boundary, keyed on `(instance, scope, step, attempt)`; resume through the same step loop | Accepted (amended by 0016) | the derived resume position breaches B8 on a long `ForEach` or a deep `SubFlow` tree, or journal payloads outgrow the generated STJ context |
| [0016](ADR-0016-postgres-journal-adapter.md) | Journal payloads are `json`, not `jsonb`; a lease carries no foreign key to the instance it precedes | Accepted | a payload needs indexing inside the journal — at which point this and 0008 re-open together |
| [0017](ADR-0017-manifest-v1-freeze-criteria.md) | Eight checkable criteria that must hold before the manifest schema is frozen at v1.0 (amends 0005) | Accepted | a criterion is closed by a check that could not have failed; P8 starts with the outbox or the policy engine still absent; the schema gains a field; or the freeze happens |
| [0018](ADR-0018-outbox-publication-and-ordering.md) | Declare `IEventPublisher` in `FlowX.Abstractions`; publish per `partition_key` in staging order and offer no global order; retention refuses to purge an instance holding a pending event | Accepted | ~~a broker plugin exists and `PublisherConformance` can hold two implementations to the contract~~ (**met at WP-56b**: `RedisStreamEventPublisher`, held to the suite alongside the recording double — the prefix return survived), or a dead-letter path is needed |
| [0019](ADR-0019-redis-lease-store.md) | A lease's expiry is a value in a hash, never a Redis key TTL; `StepScope.Root` renders as `-` at the key space | Accepted | the key space must live under an `allkeys-*` eviction policy, or a lease store is asked to carry state a journal should own |
| [0020](ADR-0020-cli-reads-the-journal-as-rows.md) | `flowx replay --mode inspect` reads the journal as rows over the published migration contract and joins it against the manifest; no journal document is published and `CliLinksNoFlowXAssembly` is not amended | Accepted | a second journal store adapter ships; or a `replay` mode has to *execute* rather than render, which this record explicitly does not decide; or the manifest freeze (0017) closes with `replay` still reading columns nothing versions |
| [0021](ADR-0021-manifest-publishes-the-wait.md) | An `AwaitSignal` step publishes `signal` and a folded `timeout`, produced and diff-classified in the change that declares them (amends 0017) | Accepted | a timer arms the declared wait, at which point `FLOWX-DIFF-206` stops being Neutral; `.Delay` produces a step; the folding set proves too small; or the freeze closes with either field unclassified |
| [0022](ADR-0022-http-shape-of-a-suspending-flow.md) | A suspending flow answers `202` with where to continue it, and its signal endpoint is generated one per (flow, signal) (amends 0004) | Accepted | an instance-status resource is built; a second transport gains a signal path; authorisation execution lands; or a timer gives a wait a fourth outcome |
| [0023](ADR-0023-policy-stages-hook-through-the-plan.md) | A policy stage is reached through `ExecutionPlan.HasStepPolicies` and a resolved `StepNode.StepPolicy`, never by walking the declared chain per step (amends 0011) | Accepted | a stage has to run *between* steps rather than around one; or `EngineAllocationTests` records a non-zero figure for an unpoliced ephemeral plan — the B2 gate this record protects; or a third flag of this shape is proposed |
| [0024](ADR-0024-stage-four-is-a-fixed-nesting.md) | Within stage 4 the four kinds nest `Retry { CircuitBreaker { Bulkhead { Timeout { capability } } } }`, by kind and never by declaration order (amends 0011) | Accepted | three documented cases need a different nesting — deliberately 0011's own bar; a fifth stage-4 kind is implemented (`Hedge` and `Fallback` are catalogued and undeclarable); or [10 §2](../10-Policy-Framework.md#2-fixed-stage-order--the-core-decision)'s `order` value is built |
| [0025](ADR-0025-a-partial-policy-engine-executes-stage-four-alone.md) | Stage 4 executes in full; stage 1, stage 3, stage 5 and stage 7's `Audit` do not, and each skip is argued separately — the duplicate-charge row of 0011's table is held shut by FLOWX1014 and a stable idempotency key, not by the stage-3 policy (amends 0011) | Accepted | stage 1, 3, 5 or the `Audit` is implemented, at which point FLOWX1032 narrows again; `Hedge` or `Fallback` becomes declarable, which would falsify "stage 4 in full"; or FLOWX1014 is downgraded from an error, which removes the first of the two mechanisms |
| [0026](ADR-0026-policy-metrics-name-only-what-executes.md) | The four [10 §9](../10-Policy-Framework.md#9-observing-policies--six-of-seven-metrics-emit) rows whose policy executes get an instrument and the three whose policy does not are left unnamed; `flowx_circuit_state` is a synchronous gauge recorded on transition rather than an observable one polled per scrape | Accepted | a §9 row's policy starts executing, at which point its name is frozen and this record's omission becomes an addition; or a second engine needs the breaker state, which a per-transition gauge cannot serve without a registry of live engines |
| [0027](ADR-0027-authorisation-runs-in-the-step-loop.md) | The authorisation check runs in the step loop, reached through `ExecutionPlan.HasAuthorizedSteps` and a resolved `StepNode.StepAuthorization` (amends 0023) | Accepted | a stance has to be decided before the flow starts rather than per step; a stance needs an asynchronous decision; or `EngineAllocationTests` records a non-zero figure for an unstanced ephemeral plan |
| [0028](ADR-0028-identity-arrives-on-the-invocation.md) | Identity reaches the engine on `FlowInvocation` as a `ClaimsPrincipal`, and `FlowContext.Principal` stops answering `null` (amends 0004) | Accepted | a second transport resolves a principal and the two disagree on claim shape; a delegation or impersonation model is needed; or `ClaimsPrincipal` stops being reachable without a package reference |
| [0029](ADR-0029-a-refusal-is-a-result-failure.md) | A refused step is a `Result` failure carrying `ErrorCategory.Forbidden`; 15 §4's `401` collapses into `403` because `ErrorCategory` is closed (amends 0007) | Accepted | the closed `ErrorCategory` set gains an authentication member — which would change the transport mapping for every existing consumer; or a caller demonstrates that it must tell "no token" from "wrong token" to act correctly |
| [0030](ADR-0030-policy-stance-is-refused-at-build-time.md) | `Authorization.Policy` is an error at build time (`FLOWX1037`), not a stance skipped at run time | Accepted | a policy evaluator abstraction ships that the engine may call without referencing ASP.NET Core, at which point `FLOWX1037` is deleted rather than downgraded |
| [0031](ADR-0031-an-occurrence-names-the-instance-it-starts.md) | A cron occurrence derives the instance id it starts, so the journal's primary key — not a leader election — is what makes a schedule fire once across every node (amends 0004) | Accepted | a schedule has to fire an instance whose id something else owns; a store weakens `StartAsync`'s duplicate refusal; a second transport needs occurrence-derived identity, at which point the derivation belongs to the abstraction rather than to the sweep; or `PerTenant` becomes declarable and one occurrence has to name many instances |
| [0032](ADR-0032-a-missed-schedule-fires-late.md) | A fire missed while every node was down happens **late**, not never, bounded by a declared catch-up horizon and narrowed by `MissedFire` (amends 0004) | Accepted | a deployment is found running with a horizon shorter than its longest outage; `RunAll` is declared on a schedule dense enough that one sweep's catch-up exceeds `MaxConcurrentRecoveries`; a durable per-schedule cursor is built, which would remove the horizon; or `Overlap` becomes executable and a late fire has to ask whether the previous one finished |
| [0033](ADR-0033-a-scheduled-flows-input-is-its-occurrence.md) | A `[CronTrigger]` flow's input contract is `ScheduledFire` — the occurrence, not the wall clock — enforced by `FLOWX1038` rather than by a silently skipped registration | Accepted | a scheduled flow needs data no occurrence carries, e.g. the tenant `PerTenant` would fan out over; `TriggerEnvelope` is populated for real, at which point `ScheduledFire` overlaps it; or a second trigger kind wants a platform-fixed input, which would make one rule out of two |
| [0034](ADR-0034-the-manifest-publishes-a-schedules-address.md) | The manifest publishes `cron` and `timeZone` and gains **no** field for `MissedFire`, `Overlap`, `Jitter` or `PerTenant`; F1's count of unproduced fields does not move (amends 0017) | Accepted | a consumer outside the application has to act on a firing policy; `PerTenant` becomes executable, which makes the fan-out an address rather than tuning; `flowx diff` gains a schedule rule these four would feed; or the freeze (0017) closes with `cron` or `timeZone` unclassified |
| [0035](ADR-0035-a-delivery-names-the-instance-it-starts.md) | A bus delivery derives the instance id it starts, so a redelivered message is refused by the journal's primary key rather than starting a second flow; `FLOWX1039` refuses a non-`Durable` subscriber (amends 0004, 0031) | Accepted | a transport delivers a message with no stable identity of its own; a subscription has to run one message twice on purpose (replay, shadow consumer); `PerTenant` becomes declarable on a bus trigger; or a store weakens `StartAsync`'s duplicate refusal |
| [0036](ADR-0036-a-message-is-acknowledged-when-its-flow-is-journalled.md) | A delivery is acknowledged once it reached a recorded outcome — success, suspension, a `Result` failure or `journal.instance_exists` — and left pending on a refusal that journalled nothing (amends 0007, 0018) | Accepted | a wait gains a firing timeout, which gives a suspended instance a terminal outcome the broker could have waited for; a store returns a refusal code the switch does not name, which falls to "acknowledge"; or a transport acknowledges asynchronously and out of order |
| [0037](ADR-0037-the-consumer-offers-per-key-order.md) | Consumption offers per-`partition_key` order and none across keys — held across a fleet by a lease per stream, not by a deployment convention (amends 0018) | Accepted | a transport partitions server-side, making the lease a second and weaker exclusion; the lease-per-stream round trip is measured as a real subscription's bottleneck; a subscription needs global order; or `event.partitionKey` becomes declarable |
| [0038](ADR-0038-a-poison-message-is-dead-lettered.md) | A consumer dead-letters an unreadable entry on first delivery and an undeliverable one past `BusMaxDeliveries`, copying it before acknowledging; the publisher side still has no dead-letter path (amends 0018) | Accepted | a dead-lettered entry has to be replayed often enough to want a `flowx` verb; the publisher side needs the same path, which changes 0018's prefix contract from "stop" to "divert and continue"; a transport carries its own dead-letter destination; or `BusMaxDeliveries` is found to be spent by node churn rather than by failure |
| [0039](ADR-0039-a-bus-subscription-publishes-no-new-manifest-field.md) | Binding `Bus` adds no manifest field: the address was already published, the tuning is deliberately absent, and `event.consumedBy` stays unproduced because a compiler can see only in-process subscribers (amends 0017, 0005) | Accepted | a tool joins several applications' manifests, at which point `consumedBy` can be produced with its `flowx diff` rule; a subscription grows an address-shaped property; `PerTenant` or a filter becomes declarable on a bus trigger; or the freeze (0017) closes with `event.consumedBy` still declared and still unproduced, which F1 then requires be **deleted** |
| [0040](ADR-0040-a-rate-limit-is-shared-or-it-is-not-a-rate-limit.md) | Stage 1 executes against `IRateLimiterStore`, a shared store with two implementations held to `RateLimiterConformance`; a process-local limiter is refused because it is **anti-conservative** — n nodes admit n × the declared rate — and a declared limit with no store registered is a refusal, never an admission (amends 0025 §2.1) | Accepted | a limit needs a scope `RateLimitScope` does not offer, which makes the key a declaration rather than a switch; a policed step's store round trip is measured as its dominant cost, which would argue for a local pre-check that can only refuse; or `Quota` becomes declarable, because a long-window budget over the same store is a second decision this record did not take |
| [0041](ADR-0041-an-idempotency-record-is-keyed-by-the-invocations-key.md) | `IIdempotencyStore` is three compare-and-set calls over a key of `ctx.IdempotencyKey` + capability id + scope; it records the state bag as of the end of the policed step, and records **only a success** — a failed step frees its key | Accepted | a flow legitimately needs two records for one capability under one key; `IdempotencyScope` gains a member, which turns a two-way discriminant into a table; or an in-flight repeat has to *wait* for the holder rather than be refused, which is a different contract |
| [0042](ADR-0042-a-recorded-result-is-replayed-only-when-recording-lost-nothing.md) | A recorded result is replayed only when recording it lost nothing: `JournalPayload.TryToReplayableJson` refuses to emit a document the redaction pass touched, and `FLOWX1040` refuses an `Idempotency` on a flow declaring a `[Sensitive]` member. `JournalPayload` gains **no** unredacted exit (amends 0025 §2.2) | Accepted | a read path for `[Sensitive]` values ships — envelope encryption, a key-management plugin — which would re-open this and the durable-resume loss it distinguishes itself from; `SensitiveMembers` stops being flow-wide; or a second policy has to record a result, `Cache` being the obvious one |
| [0043](ADR-0043-an-audit-record-is-the-journals-payload-redacted-twice.md) | An audit record carries the journal's own `JournalPayload`, so there is still exactly one exit from a value, and `redact` is a longer list of names handed to that one redaction pass; the record names which of ADR-0028's two principals authorised the step | Accepted | a delegation model needs two principals on one record; an `Ephemeral` flow needs a payload; a regime needs the record written *before* the step commits; or `redact` is asked to do anything but remove |
| [0044](ADR-0044-a-cache-is-a-plugin-store-keyed-by-the-redacted-input.md) | `IResultCache` is a plugin contract with two implementations and a conformance suite; a cache is keyed on the payload the journal would have written, and the engine declines any key or entry whose document came out carrying `[redacted]` | Accepted | single-flight is needed, which widens the contract rather than the engine; a sound build-time rule for the redacted-contract case is designed; or a cached step must be bounded separately from the capability it stands in for |
| [0045](ADR-0045-logs-emit-through-diagnosticsource-and-bridge-above-abstractions.md) | Logs emit from `FlowX.Abstractions` through a `DiagnosticListener` and `src/FlowX.Logging` bridges them to `ILogger`, so the package dependency is opt-in rather than inherited; a log event carries a `JournalPayload` and never a value (amends 0009) | Accepted | `Microsoft.Extensions.Logging.Abstractions` moves into the shared framework; a second `UnconditionalSuppressMessage` is needed under `src/`, at which point the debt gate should learn the distinction; correlating a capability's own logger becomes a requirement, which needs a scope opened above `FlowX.Runtime`; or a subscriber other than the bridge wants a payload shape `FlowLogRecord` does not have |
| [0046](ADR-0046-a-tenant-is-resolved-at-admission.md) | A tenant is resolved at **admission** in `FlowHost` — not in the step loop, and not at the transports — **derived** from validated claims rather than read off the invocation, and enforced a second time by PostgreSQL row-level security. Corrects three defects in [16 §5](../16-Multi-Tenant.md#5-data-isolation)'s DDL, which isolates nothing as written (amends 16 §3, 25 §1; relates to 0027) | Accepted | a transport needs a tenant source that is not a claim, so a bus or schedule trigger stops being refused; `TenantIsolation.Schema` is implemented and `ITenantStoreResolver` arrives; journal partitioning is specified rather than named; a tenant decision needs I/O; or `EngineAllocationTests` records a non-zero figure for a single-tenant plan |
| [0047](ADR-0047-internal-is-a-composition-stance.md) | `Authorization.Internal` means the capability is **not independently exposed** — composed into flows, never addressed on its own — so it admits every caller at the step, because a trigger addresses a flow and never a capability. The two controls its summary claimed (rejection at admission, exclusion from the agent tool surface) were never built and are deliberately not built: a tool **is** a flow, and filtering in `FlowX.Mcp` would be a second authorisation path for agents. The premise moves from a doc comment into `NoTriggerAttributeAddressesACapability`. Fixes a fail-open found alongside it: `CanRefuse` treated `Policy` as permissive because it was undecidable, so a `Policy` capability permitted everybody (amends 07 §5, 15 §4.1, 21 §3; relates to 0004, 0027, 0030) | Accepted | a transport addresses a capability directly rather than a flow, at which point the gate fails and `AdmitsEveryCaller` must lose `Internal` in the same change; an evaluator abstraction ships that lets the engine decide `Policy`; or a flow-level exposure declaration takes over the question this record leaves unanswered |
| [0048](ADR-0048-a-change-feed-advances-a-cursor.md) | A change feed **acknowledges nothing**; it advances a cursor over `staged_xid` past the longest prefix that reached a recorded outcome, reading only rows below `pg_snapshot_xmin(pg_current_snapshot())` — because `staged_seq` is allocated before commit and a cursor on it skips gaps for ever (amends 0018, 0036) | Accepted | a feed ships over a store with no snapshot barrier; a subscription needs more throughput than one node, which is what decision 4 costs; retention learns about cursors; or a change is seen stalling behind a refusal this record calls transient |
| [0049](ADR-0049-a-change-names-the-instance-it-starts.md) | An observed change derives the instance id it starts, as 0035 does for a delivery, with a scope term so a change subscription and a bus subscription over one type and group stay two subscribers (amends 0031, 0035) | Accepted | a feed delivers a change with no stable identity of its own; a subscription must observe one change twice on purpose; or a fourth transport needs derived identity, at which point the scope term belongs in `DerivedIdentity` |
| [0050](ADR-0050-a-change-trigger-observes-the-outbox.md) | `[ChangeTrigger(type, Group = …)]` observes the **outbox** — the change feed FlowX itself produces — reading `outbox_event` without writing it, so a change subscription and the outbox publisher coexist over one table; the manifest publishes the existing `topic` and `group` and no new field (amends 0004, 0018, 0039) | Accepted | a second `IChangeFeed` ships and "the source is an event type" stops being general enough; retention learns about change cursors; an indirect emit/observe cycle is seen in production, which makes decision 3 a compile-time rule; or a subscription must observe more than one type |
| [0051](ADR-0051-database-isolation-is-a-topology-not-a-runtime-level.md) | `TenantIsolation.Schema` is a **bounded connection pool per tenant**, each pointed at that tenant's schema by its connection string rather than by a `SET` — so a pooled connection has no other tenant to carry a `search_path` to — with the schema provisioned and migrated on first use and both node-wide sweeps fanned out across schemas. No `ITenantStoreResolver`: the map lives in the plugin and the abstraction gains `ITenantScopedJournal.Isolation`, so a host refuses a level its store cannot serve. `Database` **stays refused**: it names a deployment per tenant, whose pod declares `None` (amends 16 §5, 25 §1; relates to 0046) | Accepted | a broker plugin exists and per-tenant outbox publication has to be decided; `flowx tenant purge` is built, at which point de-provisioning arrives; or a second store implements `ITenantScopedJournal` with its own notion of a schema |
| [0052](ADR-0052-retention-is-owed-to-a-declared-consumer.md) | An outbox row is retained until **every consumer the deployment declares** is past it: the publisher's progress is `published_at`, a change subscription's is its `change_cursor` row, and one predicate carries both into the instance purges and the published-row window. The set is `RetentionConsumers`, stated at registration because a cursor is cluster-wide; the default is one publisher and no subscriptions, which is what 0018 shipped (amends 0018 §5, 0050) | Accepted | a second `IChangeFeed` exists, so "a cursor in this schema" stops being general enough; or cursor lag becomes a metric, at which point a dead subscription could stop holding rows |
| [0053](ADR-0053-the-outbox-fans-out-and-the-change-feed-cannot-yet.md) | The **outbox publisher fans out** across tenant schemas at `TenantIsolation.Schema` — a claim was always confined to one table, so `SKIP LOCKED` and the per-key hold answer the same question over less data — with the tenant set re-read each pass, a rotating visit order and a failing tenant stepped over rather than ending the pass; `OutboxRecord.TenantId` carries whose schema a row came from. The **change feed stays refused, for a different reason**: its cursor and the cluster-wide visibility barrier both survive the split, but a change must start a flow in its tenant and a change scan carries no principal, so the host refuses with `tenant.required` — which a scan counts as progress, moving the cursor past work that never ran (supersedes 0051 §4) | Accepted | a platform-initiated trigger gains an admission path carrying a tenant without a continuation's authorisation bypass; or a broker plugin ships and per-tenant concurrency in one pass has to be bounded |
| [0054](ADR-0054-a-platform-trigger-attests-its-tenant.md) | A trigger with no caller **attests** its tenant: `FlowInvocation.TenantAttested` carries a value derived from something no caller can set — the schema and emitting instance a change was read from, the broker field a publisher wrote, the `ITenantDirectory` a `PerTenant` occurrence was fanned out over — and is deliberately not `IsContinuation`, which also skips step authorisation. A trigger that cannot name a tenant **holds**: the change cursor does not advance, the message is not acknowledged, the occurrence counts as failed. `AddFlowXPostgresChangeFeed` fans out at `Schema` (supersedes 0053 §2 on its own revisit condition; relates to 0028, 0031, 0046) | Accepted | a store gains a tenant registry at row isolation, making `FlowXOptions.Tenants` a fallback rather than the answer; or one change-feed pass per tenant is measured to be the bottleneck |
| [0055](ADR-0055-a-window-names-the-instance-it-starts.md) | A stream subscription's checkpoint is the source position of the longest prefix of admitted records whose windows have **closed and reached a recorded outcome**, and **window state is not journaled**: a restart re-reads from it and rebuilds identical windows, because assignment is a pure function of event time aligned to the epoch. A closed window derives the instance id it starts (0031's derivation, fourth use), so a rebuilt window is refused by `flow_instance`'s primary key rather than aggregated twice — which makes `Streaming` a journaled profile. Costs: re-reading after a node death, an at-least-once side output, a stream trimmed past the checkpoint **stops** the subscription, one reader per subscription, memory bounded by refusing, and **tumbling windows only** | Accepted | an incremental-aggregation contract exists in the DSL, making a window's state one value rather than a list of records |
| [0056](ADR-0056-the-watermark-is-observed-never-wall-clock.md) | The watermark is `max(observed event time) − Lateness`, monotonic, and **nothing else moves it** — no wall clock, no idle timeout, no scan interval. A wall-clock watermark is `FLOWX1007`'s ambient read taken by the engine instead of by the flow, and it would make two replays close different windows and derive different ids, which is the precondition 0055 rests on. Cost: a stream that goes quiet leaves its last window open indefinitely, and one that stops for good never emits it — visible as a subscription making no progress rather than as a silent partial aggregate | Accepted | a deployment needs an idle-close and can state what a window computed from a partial input means |
| [0057](ADR-0057-a-write-budget-is-drawn-in-blocks-and-paces-rather-than-refuses.md) | A tenant's **journal write budget** is a bucket of *blocks* in `IRateLimiterStore`: a node draws `JournalWriteBlock` rows of credit in one call and spends them locally, so the fleet total is the shared bucket's and the per-row cost is an interlocked decrement. Not a cache — the credits leave the bucket before any of them can be spent, so the error is unspent budget rather than n × the declared rate (0040). **It cannot be charged at admission**: a `ForEach`, a retry and a backward `Jump` all write rows the plan does not count. Exhaustion **paces** rather than refuses, because refusing a mid-flow commit strands a saga with its compensations unrun — the distinction `FlowHost.AdmitAsync` already draws for continuations (relates to 0040, 0046) | Accepted | a write budget must be keyed by something other than the tenant; the unspent remainder is measured to matter, which would argue for returning credit at the end of a window; or `IRateLimiterStore` gains a cost parameter, at which point the bucket can be denominated in rows |

## Writing an ADR

```markdown
# ADR-NNNN: <Decision in active voice>

**Status:** Proposed | Accepted | Superseded by ADR-MMMM
**Date:** YYYY-MM-DD
**Deciders:** <roles>

## Context
Forces at play: constraints, quality goals affected, options considered
(rejected options: one line each with the rejection reason).

## Decision
We will <decision>, because <primary driver>.

## Consequences
**Positive:** …
**Negative / accepted trade-offs:** …
**Revisit when:** <condition> (mandatory)
```

An ADR with no "Negative" section has not been thought through. An ADR with no
"Revisit when" is a decision nobody can ever safely change.

*Two records broke that rule and were brought to it on 2026-07-31, in the way the rule
implies rather than by adding headings: **ADR-0013**'s "Reversal condition" was renamed
`Revisit when`, because it was already one, and **ADR-0016** gained the Positive / Negative
split its trade-offs had been argued under other headings all along. Both records say which
was done and why. A third obligation this table carries is the "Revisit when" column itself
— it is where a reader checks whether a decision needs reopening, so a condition summarised
here without its measurable half, as **0002**'s was, hides exactly what the column exists to
show.*
